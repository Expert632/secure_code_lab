name: 🔐 Secret Scan CI

# Déclenchement à chaque push sur main ou PR
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  detect-secrets:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Récupérer le code
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # nécessaire pour scanner tout l'historique

      # 2️⃣ Scanner le code pour les secrets
      - name: Scan for secrets with Gitleaks
        uses: zricethezav/gitleaks-action@v2
        with:
          args: detect --source . --no-git -v --report-format=json --report-path=gitleaks-report.json

      # 3️⃣ Optionnel : générer un rapport JSON
      - name: Save Gitleaks report
        if: always() # toujours exécuter, même si secrets détectés
        uses: actions/upload-artifact@v3
        with:
          name: gitleaks-report
          path: gitleaks-report.json
